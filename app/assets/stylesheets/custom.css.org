@import "bootstrap-sprockets";
@import "bootstrap";

$light-gray: #777;
$gray-medium-light: #eaeaea;


/* universal */

body {
  padding-top: 40px;
  font-size: 16px;
  font-family: "AdelleSans-Regular",Karla,sans-serif;
  font-style: normal;
  font-weight: 400;
  color: #44413f;
  line-height: 1.375;
}

section {
  overflow: auto;
}

textarea {
  resize: vertical;
}

.center {
  text-align: center;
  h2 {
  margin-bottom: 10px;
  }
}

.page-container {
  min-height: 100%;
  display: flex;
  flex-direction: column;
  background-color: #f4f4f4;
}

/* typography */

h1, h2, h3, h4, h5, h6 {
  line-height: 1;
  font-style: italic;
}

h1 {
  font-size: 3em;
  letter-spacing: 2px;
  margin-bottom: 30px;
  text-align: center;
}

h2 {
  font-size: 1.2em;
  letter-spacing: -1px;
  margin-bottom: 30px;
  text-align: center;
  font-weight: normal;
  color: $light-gray;
}

p {
  font-size: 1.1em;
  line-height: 1.7em;
}

/* header */

header {
  position: relative;
  .container {
    display: flex;
    justify-content: space-between;

    .header-logo {
      #logo {
        margin-right: 10px;
        font-size: 1em;
        color: black;
        text-transform: uppercase;
        letter-spacing: -1px;
        font-weight: bold;
        &:hover {
          color: black;
          text-decoration: none;
        }
        img {
          position: absolute;
          height: 100%;
        }
      }
    }
    li {
      text-align: end;
      a {
        text-decoration: none;
        font-size: 14px;
      }  
    }
  }
  
  
}

nav {
  position: sticky;
  top: 0;
  background-color: #fff;
  z-index: 5000;
  box-shadow: rgba(51,51,51,0.28) 2px 4px 3px -2px;
  .nav {
    display: flex;
    justify-content: flex-end;
    a {
    flex-direction: row;
    }
  }
  
}




/* footer */

footer {
  margin-top: 45px;
  padding-top: 5px;
  border-top: 1px solid #eaeaea;
  color: $light-gray;
  a {
   color: #555;
  }
  a:hover {
  color: #222;
  }
  ul {
  float: right;
  list-style: none;
  }
  small {
  float: left;
  }
  ul li {
  float: left;
  margin-left: 15px;
  }
}

/* mixins, variables, etc. */
@mixin box_sizing {
  -moz-box-sizing:    border-box;
  -webkit-box-sizing: border-box;
  box-sizing:         border-box;
}

@mixin flex_page {
  flex: 1;
}

/* miscellaneous */

.debug_dump {
  clear: both;
  float: left;
  width: 100%;
  margin-top: 45px;
  @include box_sizing;
}

/* forms */

input, textarea, select, .uneditable-input {
  border: 1px solid #bbb;
  width: 100%;
  margin-bottom: 15px;
  @include box_sizing;
}

input {
  height: auto !important;
}

.checkbox {
  margin-top: -10px;
  margin-bottom: 10px;
  span {
    margin-left: 20px;
    font-weight: normal;
  }
}

#session_remember_me {
  width: auto;
  margin-left: 0;
}

/* forms */

#error_explanation {
  color: red;
  ul {
    color: red;
    margin: 0 0 30px 0;
  }
}

.field_with_errors {
  @extend .has-error;
  .form-control {
    color: $state-danger-text;
  }
}

/* Users index */

.users {
  list-style: none;
  margin: 0;
  li {
    overflow: auto;
    padding: 10px 0;
    border-bottom: 1px solid $gray-lighter;
  }
}

/* Pagination */

.pagination {
  text-align: center;
}

/* section parts */
@mixin card {
  border-color: #f4f4f4;
  border-style: solid;
  border-width: 0 0 1px;
  border-radius: 4px;
  box-shadow: 0 18px 36px -4px rgba(199,199,199,0.44);
}
@mixin horizontal_division {
  width: 48%;
  background-color: #fff;
  display: flex;
  flex-direction: column;
  align-items: center;
}
@mixin division_line {
  margin: 20px 0;
  border-color: #f4f4f4;
  border-style: solid;
  border-width: 1px 0 0;
  width: 100%;
}

/* Top Page */

.product-wrapper {
  display: inline-flex;
  align-items: flex-end;
  flex-direction: row;
  flex-wrap: wrap;
  padding: 0 3em;
  .grid {
    align-items: flex-start;
    padding-left: 24px;
    margin-bottom: 60px;
    display: inline-block;
    width: 25%;
    .product-card {
      position: relative;
      display: block;
      background-color: #fff;
      text-align: center;
      padding: 0;
      color: #44413f;
      z-index: 1;
      margin-bottom: 20px;
      overflow: hidden;
      transition: transform 300ms cubic-bezier(0.215, 0.61, 0.355, 1),box-shadow 300ms ease-in-out;
      display: inline-block;
      text-decoration: none;
      font-size: 14px;
      @include card;
      img {
        top: 0;
        left: 0;
        height: 100%;
        width: 100%;
        border-style: none;
        line-height: 300px;
      }
      .product-info {
        text-align: center;
        padding-bottom: 5px;                
      }
      
      
    }
  }
}

/* Product show */
.product {
  width: 90%;
  margin: 0 auto;
  display: flex;
  justify-content: space-between;
  padding: 5em 0;
  .half-section_left {
    @include horizontal_division;
    @include card;
    justify-content: center;
    .product-image {
      max-width: 90%;
      margin: 0 auto;

    }
  }
  .half-section_right {
    @include horizontal_division;
    @include card;
    .product-info {
      max-width: 90%;
      margin: 0 auto;
      padding: 2em;
      .product-name {
        font-family: "Work Sans",sans-serif;
        font-weight: 900;
        text-transform: uppercase;
        text-align: left;
      }
      .product-price {
        font-size: 1.5em;
        padding-bottom: 1em;
      }
      .product-comment {
        background-color: #fff;
      }
      
    }
    hr {
      @include division_line;
    }
    .product-menus {
      display: flex;
      flex-wrap: wrap;
      padding: 2em;
      .input_number {
        padding: 5px;
        p {
          display: block;
          width: 100%;
          margin: 0;
        }
      }
    }
    p {
      text-align: center;
    }
    .button-wrapper{
      width: 100%;
      text-align: center;
      padding-bottom: 2em;
      .button {
        width: 300px;
        padding: 12px 5px;
        letter-spacing: 2px;
        margin: 0 auto;
        color: #fff;
        background-color: #428bca;
        border-color: #357ebd;
        border-style: solid;
        border-width: 0 0 1px;
        border-radius: 4px;
      }
    }
    
  }
}

/* Cart */
.cart_products {
  background-color: #fff;
  @include card;
  tr {
    .image {
      width: 25%;
    }
  }
  
}


/* About */
.about-main {
  @include flex_page;
}


/* Button */

